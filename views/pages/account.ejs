<!DOCTYPE html>
<html>

  <head>

      <% include ../partials/header.ejs %>
	</head>
	
	<script>
		function loadPage() {
			getPerson();
			getGames();
		}
		function getPerson() {
			var person = $("#person");
			$.get('/getPerson',
				0,
				function callback(data, status) {
					if(status != "success") {
						console.log(status);
					} else {
						person.append('<div class="row pad">');
						person.append('<label for="name">Name: ' + data.result.name + '</label>');
						person.append('</div>');
						person.append('<div class="row pad">');
						person.append('<label for="name">Street: ' + data.result.street + '</label>');
						person.append('</div>');
						person.append('<div class="row pad">');
						person.append('<label for="name">City: ' + data.result.city + '</label>');
						person.append('</div>');
						person.append('<div class="row pad">');
						person.append('<label for="name">State: ' + data.result.state + '</label>');
						person.append('</div>');
						person.append('<div class="row pad">');
						person.append('<label for="name">Zip' + data.result.zip + '</label>');
						person.append('</div>');
						person.append('<div class="row pad">');
						person.append('<label for="name">Card Number' + data.result.card_num + '</label>');
						person.append('</div>');
					}
				})
		}
	</script>

  <body onload="loadPage()">
  
    <header>
      <h1 class="text-center title"> <span class="red">Rager</span>Gamer</h1></br>

      <p class="text-right"><!--?php echo "Welcome " . $_SESSION['username']?--></p>
      <div class="pull-right">
        <a href="/browse">Back to browse</a>
        <a href="/updateInfo">Update Info</a>
        <a href="/logout">Logout</a>
        
      </div>  
      </br></br></br>

      <hr class="thick">

    </header>

  <h1 class="text-center">Welcome to your account!</h1>


  <h4 class="text-center">Account Details</h4>

  <div id="person" class="col-xs-12 text-center">

  </div>

  <h4 class="text-center">Here are all of the games you've bought!</h4>

  <!--use node to list games that have been sold and bought-->

  </body>